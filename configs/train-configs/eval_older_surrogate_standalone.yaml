global:
  device: 0
  mode: train  # ['train', 'eval']
  formulation: p(y|y_sparse_benchmark)

model:
  name: older_surrogate_mh
  pretrained: True
  weights: 
  config:

training:
  batch_size: 1
  steps_per_epoch: 1024
  epochs: 500
  loss: MSE
  lr: 1e-5
  optimizer: Adam

validation:
  batch_size: 1
  steps_per_epoch: 1000
  loss: L1

dataset:
  name: MOS2SefOLDERSurrogate
  image_size: 512
  crop_size: 128
  num_workers: 0
  masking_ratio: 1

logging:
  root: "/playpen/mufan/levi/tianlong-chen-lab/material-super-resolution/__exps__/y-task-formulations/p(y | y_sparse)/e. surrogate standalone train-runs"
  exp_name: older-surrogate
  result_columns: [""]
  save_weights: true
  save_only_best_weights: true
  enable_tensorboard: false
  log_figures: false
  log_interval: 100